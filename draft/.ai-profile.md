---
project: "image-s3-proxy"
module: "root"
generated_by: "draft:init refresh"
generated_at: "2026-06-16T08:20:05Z"
git:
  branch: "main"
  remote: "origin/main"
  commit: "6f2e71a3c70d732b573801747ca0435698c9d0e5"
  commit_short: "6f2e71a"
  commit_date: "2026-06-16 00:48:55 +0200"
  commit_message: "chore: bring draft/tracks.md frontmatter up to current HEAD"
  dirty: false
synced_to_commit: "6f2e71a3c70d732b573801747ca0435698c9d0e5"
---

# image-s3-proxy — Profile

## Stack
lang:        Go 1.19 (CGO required for libvips)
http:        net/http stdlib (no router); 3 regex URL families + POST /_/worker/trigger
storage:     AWS SDK v2 → S3-compatible. Production: Hetzner Object Storage; R2 canary via CACHE_MODE shadow/live
image:       govips v2.16 (libvips wrapper)
observability: stdout JSON access log (incl. per-phase timings) + Server-Timing response header
test:        make test (Alpine Docker) is full-stack source of truth; host `go test` for libvips-free packages
deploy:      Hetzner dedicated + k3s; Docker (alpine default, debian alt) via CircleCI → Docker Hub
entry:       cmd/image-proxy/main.go:main
build:       make build (== docker compose build app, alpine)

## INVARIANTS (top 5)
- BUCKET env required; missing → log.Fatal @main.go
- CACHE_MODE != off requires CACHE_BUCKET; fatal otherwise @main.go
- Cache-back uses putBoth: off=single Put; shadow=origin→cache; live=cache→origin; per-side failures logged, never abort @server.go
- POST /_/worker/trigger: validate synchronously, 400 on first failure, 202 BEFORE detached goroutine starts @server.go:handleWorkerTrigger
- access-log Entry always has top-level "timings" key; upstream.responseTime == sum(timings) within float ε @accesslog/middleware.go

## NEVER
- Do not introduce S3 Tagging on PutObject — silently no-ops on HOS, hard-fails on R2 (IMAGE_TAGS is deprecated)
- Do not call os.Getenv outside cmd/image-proxy/main.go
- Do not call vips.Startup outside resizer.LibvipsResizer.Startup

## Active Tracks
(none — all 4 tracks merged: PRs #1-#4)

## Recent Changes
- 6f2e71a chore: bring draft/tracks.md frontmatter up to current HEAD
- 0ad560e feat: extend worker trigger to multi-image / multi-format batches (#4)
- 827e5a5 feat: include per-phase timings in JSON access log (#3)
- 9ef919f feat: split origin/cache buckets + canary mode (off/shadow/live) (#2)
- 2718fce feat: structured access logs + Server-Timing header (#1)
